import React, { useEffect, useRef } from 'react';


function About({ onEnter, }) {
  const ref = useRef(null);

  return (
    <section ref={ref} id="about" className='relative z-0'>
      <div className="">
        
      </div>     
    </section>
  );
}

export default About;